apply plugin: "com.android.application"

android {
  namespace "com.libsdlui.test"

  defaultConfig {
    applicationId "com.libsdlui.test"
    compileSdk 34
    minSdkVersion 29
    targetSdkVersion 34
    versionCode 1
    versionName "1.0.0"
  }

  sourceSets.main {
    jniLibs.srcDir "libs"
  }
}
